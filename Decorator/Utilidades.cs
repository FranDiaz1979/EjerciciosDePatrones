namespace Decorator
{
    using System;

    class Utilidades
    {
        public static Object RequestDataBase(string consultaSQL)
        {
            consultaSQL.ToString();
            return null;
        }
    }
}
